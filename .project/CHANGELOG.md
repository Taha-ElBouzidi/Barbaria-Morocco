# Changelog

Real-time log of non-trivial actions on this codebase. Every commit, decision, scaffold, or scoping move gets one entry. Newest first.

Format: `YYYY-MM-DD HH:MM TZ — <one-line summary>`

---

## 2026-05-12

- 23:30 CET. Sprint 1.5 brand refresh + catalogue alignment implemented on branch `feat/brand-refresh-sprint-1-5`. Replaced 17 invented seed products with the client's real 36 SKUs from the B2B catalogue PDF (6 gammes × 6 products), added 3 curated Heritage packs (pack-nila-oranger, pack-rose-aker-fassi, pack-rituel-naturel) referencing real products via `related`. Updated subcat taxonomy to match the 6 gammes (scrubs/masks/soaps for Hammam; oils/serums/hydrolats/massage for Botanical; packs for Heritage). Reworked /story page with 4 Histoire chapters (Antiquité, Médiéval, Tradition, Aujourd'hui) plus a 5th Tifinagh chapter with 6 Amazigh symbols (Yaz/Yay/Yak/Yam/Yan/Yar; letter names corrected from the mockup's mix of Tifinagh + Arabic naming). Added `<AmazighProverb>` block (« Nul n'est étranger sur la terre de ses ancêtres. ») rendered on both Story and Contact. Hero eyebrow swapped to "Authentique · Naturel · Éthique" (catalogue tagline), lede swapped to "L'Art du Terroir & les Rituels Ancestraux du Maroc" (mockup tagline). Added `<TifinaghMark>` primitive (ⵣ ambient brand mark). Extended seed with `purgeOrphans()` that NULLs FK refs before dropping orphan subcats. 39 products in DB, 78 translations, build green, types clean. Spec: `.project/specs/2026-05-12-brand-refresh-sprint-1-5-design.md`. Files touched: lib/products.ts (rewrite), lib/rituals.ts (rewrite), db/seed.ts, messages/{en,fr}.json, app/[locale]/{story,contact}/page.tsx, new components AmazighProverb + TifinaghMark.
- 21:45 CET. PR #13 merged: logout-on-nav bug fixed at the actual root cause. TopBar's `<Link href="/admin/logout">` was being prefetched by Next.js on dashboard mount; the route's GET handler called signOut() and destroyed the session before any click. Fix: `components/admin/TopBar.tsx` now uses `<form action="/admin/logout" method="POST">`, and `app/admin/(auth)/logout/route.ts` exports only POST (GET now 405s by design). Middleware and cookie work from PRs #9/#11/#12 were correct all along, just incomplete. Memory entry added: `nextjs-link-prefetch-side-effects.md`. Build green.
- 14:30 CET. Admin auth switched from magic-link to email+password. Supabase free-tier SMTP rate limit (2/hour) was unusable; rather than configure custom SMTP, opted for password auth which removes the email dependency entirely. Password managed manually in Supabase Dashboard (no signup or reset flow on our site for v1). Files changed: app/admin/(auth)/login/{page,actions}.ts rewritten, app/admin/(auth)/auth/callback/route.ts deleted, proxy.ts public-paths trimmed, tests/admin-smoke.spec.ts updated. Build exit 0. 50/50 tests green.

- 14:00 CET. Slice 17: admin smoke + axe tests. tests/admin-smoke.spec.ts (login form render + error/sent states + invalid-email guard + 5 route-guard redirect tests). tests/admin-a11y.spec.ts (axe scan on /admin/login, zero serious/critical). Fixed 3 source issues uncovered by test run: (1) app/admin/layout.tsx called requireAdmin() which redirected /admin/login causing infinite loop — replaced with getCurrentAdmin() + conditional AdminShell render; (2) app/admin/layout.tsx was missing html/body tags causing NEXT_MISSING_ROOT_TAGS error and html-has-lang axe violation — added html[lang=en]+body wrapper; (3) error text in login/page.tsx used text-bb-tertiary (#b45f42, contrast 4.29:1, fails WCAG AA) — switched to text-bb-primary (#1b3022, contrast 13.39:1). Authenticated admin coverage deferred to Sprint 2.5 per backlog note — the auth fixture is brittle and adds risk in this sprint. Test count: 40 public + 9 admin smoke + 1 admin axe = 50 green.

- 23:55 CET. Slice 16: admin inquiries (read-only) + activity log view. Inquiries inbox at /admin/inquiries (empty state in Sprint 2; Sprint 3 wires the form to populate). Status filter chips (All/New/Contacted/Quoted/Won/Lost) + sort toggle (newest/oldest) as server-side querystring params. Inquiry detail at /admin/inquiries/[id] shows full company/contact/items breakdown — status changes and notes editor deferred to Sprint 3. Activity log at /admin/activity reads audit_log with entity-type + action + date-range filters and pagination (50/page). Actor emails resolved from admin_users via a follow-up query; null actor_id displays as "system". ActivityLogTable is a client component (useState expand-to-diff per row); AuditEntryRow renders the expandable diff. Files: lib/admin/inquiries.ts, lib/admin/activity.ts, app/admin/inquiries/page.tsx, app/admin/inquiries/[id]/page.tsx, app/admin/activity/page.tsx, app/admin/_components/ActivityLogTable.tsx, app/admin/_components/AuditEntryRow.tsx. Build exit 0. 40/40 tests green.
- 23:30 CET. Slice 15: admin CRUD for journal cards, ateliers, rituals + sub-categories, facets. Journal + atelier follow products' pattern (list/new/[id]/_components). Rituals page is read-mostly (3 fixed rows; can edit translations + hero + sub-cats, can't add/delete rituals themselves). Sub-cat delete blocked if any products reference it (count shown). Facets: single-page editor with 5 axis sections; delete blocked if products use the facet. All mutations revalidatePath affected public routes. Audit log auto-fills via DB triggers. Sidebar extended with Facets link. Files: lib/admin/journal.ts, lib/admin/ateliers.ts, lib/admin/rituals.ts, lib/admin/facets.ts + app/admin/journal/*, app/admin/ateliers/*, app/admin/rituals/*, app/admin/facets/*.
- 22:30 CET. Slice 14: admin products CRUD. /admin/products list (client-side search + ritual + status filters, row-click navigation). /admin/products/new and /admin/products/[id] share ProductEditor: identity (slug, ritual, subcategory, MOQ, formats, lead, origin, ritual_label, hero flag), EN+FR translation tabs, multi-select facet chips per axis, image upload (edit page only; new form shows "save first" prompt), up to 3 application steps with EN/FR fields. Server actions saveProduct/setStatus/deleteProduct in app/admin/products/[id]/actions.ts use service role client + Zod v4 validation + revalidatePath for affected public routes. /api/admin/images (POST multipart upload to Supabase Storage + DB insert, DELETE storage + DB) and /api/admin/images/reorder (PATCH sort_order) wrapped with requireAdmin(). lib/admin/products.ts: listProductsForAdmin, getProductForEdit, getAllFacetsForAdmin, getRitualOptions, getSubcatOptions, ProductSaveSchema. Image up/down reorder via buttons (drag-reorder deferred to Sprint 2.5). Build exit 0. 40/40 tests green.
- 21:30 CET. Slice 13: admin shell + dashboard. app/admin/layout.tsx now calls requireAdmin() and renders AdminShell (sidebar + topbar + content). Auth routes (login, auth/callback, logout) moved into app/admin/(auth)/ route group with their own minimal layout so they bypass the shell — route group leaves /admin/login URL unchanged. Dashboard at /admin: 4 stat tiles (published products, drafts, ateliers, journal entries) + quick-action buttons for Slice 14 flows + recent activity feed pulling last 10 audit_log entries. Sidebar nav covers 7 admin sections with active-state highlighting. TopBar shows display name + role + sign-out. Build exit 0. Route table shows /admin, /admin/login, /admin/auth/callback, /admin/logout. 38/40 Playwright tests green — 2 pre-existing failures on public /en and /product/cedar-box unrelated to admin (confirmed identical failures on base commit e709a2e).
- 20:45 CET. Slice 12: swapped every public route + sitemap + inquiry drawer/sidebar from lib/products+lib/rituals+lib/editorial TS imports to lib/data/* async DB reads. Public routes are now async RSC with revalidate=60 ISR safety net. ProductCatalogueProvider (new) hydrates inquiry drawer/sidebar with a server-fetched minimal product map per request. generateStaticParams uses a cookie-free createClient directly (avoids cookies() in build context). 40/40 Playwright tests green (single-worker; parallel-worker runs show transient Turbopack cold-start 500s that vanish on retry — pre-existing dev-server behavior, not a code bug). lib/products.ts + lib/rituals.ts + lib/editorial.ts remain as seed sources until Sprint 2.5.
- 19:10 CET. Slice 11: lib/data/{products,rituals,ateliers,journal,facets}.ts + lib/data/types.ts. Server-side helpers reading from Supabase via createServerClient (anon-keyed, RLS-gated). Returns locale-resolved flat shapes ({name: string, short: string} rather than {name: {en, fr}}) — Slice 12 must adapt consumers. getProductBySlug, getProductsByRitual, getAllProductSlugs, getMinimalProductMap for products. getAllWorlds, getWorld, getSubcatsForWorld for rituals. getAllAteliers, getAllJournalCards, getAllFacets for editorial pages. Build exit 0.
- 18:30 CET. Slice 10 run: seed executed against Supabase. Counts verified: 3 rituals, 6 ritual translations, 15 subcategories, 30 sub translations, 36 facets, 17 products, 34 product translations, 20 product images, 55 product-facet links, 6 ateliers, 12 atelier translations, 6 journal cards, 12 journal translations. 83 audit_log entries confirm triggers fired on every insert. Service role key pasted in chat by Taha; advised to rotate immediately post-verification.
- 18:00 CET. Slice 10: db/seed.ts written. One-shot idempotent seed: 3 rituals + 6 translations + 15 subcats + 30 sub translations + ~40 facets + 17 products + 34 product translations + product_images + product_application_steps + product_facets links + 6 ateliers + 12 atelier translations + 6 journal_cards + 12 journal translations. npm run seed via tsx. dotenv installed as devDep. Pending real SUPABASE_SERVICE_ROLE_KEY in .env.local before controller runs.
- 17:05 CET. Slice 9: /admin/login (server-action form), /admin/login/actions.ts (signInWithMagicLink + zod email validation, host-derived emailRedirectTo), /admin/auth/callback (exchanges code, verifies admin_users membership, auto-bootstraps first admin if BOOTSTRAP_ADMIN_EMAIL matches and table is empty), /admin/logout (POST + GET), app/admin/layout.tsx (minimal, no requireAdmin to avoid login-loop). .env.example documents BOOTSTRAP_ADMIN_EMAIL with placeholder; .env.local set to ta.elbouzidi@gmail.com. Build exit 0. 40/40 tests green.
- 16:10 CET. Slice 8: proxy.ts now wraps next-intl middleware with a Supabase session refresh and admin route guard. /admin/* (except /admin/login and /admin/auth/callback) require a session whose user is in public.admin_users; otherwise redirect to /admin/login. Signed-in non-admins are signed out then redirected to /admin/login?error=unauthorized. Public routes fall through to next-intl unchanged, with Supabase session-refresh cookies merged into the intl response. Matcher excludes /api, _next internals, and files with extensions. Build green (exit 0). 40 Playwright tests still green.
- 15:30 CET. Slice 7: lib/supabase/{server,browser,service}.ts factories + lib/admin/auth.ts with getCurrentAdmin() and requireAdmin() helpers. Service-role client throws if SUPABASE_SERVICE_ROLE_KEY is unset (server-only protection). requireAdmin() redirects to /admin/login if no session or no admin_users row.
- 14:50 CET. Slice 6: storage bucket product-images created (8 MB limit, jpeg/png/webp/avif MIME allowlist, public read). 4 RLS policies on storage.objects scoped to bucket + admin_users membership. Migration files 0003_storage.sql + 0004_lockdown_log_audit.sql committed for repo reproducibility.
- 14:45 CET. Security audit fix: log_audit() SECURITY DEFINER function was auto-exposed as /rest/v1/rpc/log_audit to anon + authenticated roles (advisor WARN). Revoked EXECUTE from anon/authenticated/public. Triggers still fire (no EXECUTE grant required for trigger invocation). get_advisors(security) back to zero lints.
- 14:30 CET. Slice 5 apply: 0002_triggers migration applied to Supabase. moddatetime extension enabled, 5 updated_at triggers, log_audit() SECURITY DEFINER function, 7 audit triggers active. Smoke test (insert + update + delete a temp ritual) confirms audit_log captures all 3 operations.
- 14:10 CET. Slice 5: db/migrations/0002_triggers.sql written. moddatetime extension enabled. 5 BEFORE UPDATE triggers maintain updated_at on rituals/ateliers/products/journal_cards/inquiries. log_audit() SECURITY DEFINER plpgsql function + 7 AFTER triggers track every INSERT/UPDATE/DELETE on tracked entities into audit_log (entity_type passed as trigger argument). Pending MCP apply by controller.
- 13:30 CET. Slice 4 apply: 0001_rls migration applied to Supabase via MCP. RLS enabled on all 18 public tables, 33 policies created. `get_advisors(security)` returns zero lints (down from 18 critical RLS-disabled warnings post-Slice 3).
- 11:56 CET. Slice 4: db/migrations/0001_rls.sql written. 18 ENABLE RLS statements + 33 policies (anon read on public catalogue with status='published' filter on products + journal_cards; admin read/write on catalogue; admin read on audit_log + inquiry_items; admin read + update on inquiries; admin all on admin_users). Pending MCP apply by controller.
- 13:15 CET. Slice 3: migration 0000_initial applied to Supabase via MCP `apply_migration`. 18 public tables created, 8 enums, 13 intra-schema FKs, 8 cross-schema FKs to auth.users, 10 indexes. `list_tables` confirms all 18 present with 0 rows; `get_advisors` flags expected RLS-disabled critical (resolved in Slice 4).
- 13:00 CET. Slice 2: Drizzle schema for 18 tables (3 rituals + 5 products-related + 5 ateliers/journal + 5 inquiry/admin/audit + 8 enums + relations). Migration 0000_initial.sql generated (drizzle-kit starts at 0000). Hand-edited to add 5 ALTER TABLE blocks (8 individual FK constraints) to auth.users for admin_users.id, products.created_by/updated_by, journal_cards.created_by/updated_by, inquiries.assigned_to, audit_log.actor_id. Build green.
- 12:05 CET. Slice 1: installed @supabase/supabase-js + @supabase/ssr + drizzle-orm + postgres + zod + drizzle-kit + tsx. Scaffolded .env.example, .env.local (gitignored), drizzle.config.ts.
- 11:00 CET. Sprint 2 kickoff: spec at `.project/specs/2026-05-12-sprint-2-admin-db-design.md`. Supabase project `jnparcnvkghiuryarbac` (BARBARIA DASH, eu-west-1) provisioned, MCP authenticated, public schema empty and ready for migrations. Phase A scope: products/journal/ateliers CRUD admin + read-only inquiry inbox + audit log.

---

## 2026-05-11

- 20:00 CET. Polish 2: hero photo swapped to user-supplied source (hero-atlas.jpg, 194 KB). MenuDrawer Corporate section trimmed: removed Bespoke Customisation and Logistics & Lead Times rows plus their i18n keys; kept B2B Concierge.
- 19:30 CET — Post-launch polish: header active state, em-dash audit, rich MenuDrawer (icons + Discover/Corporate sections + CTA), CredentialStrip marquee, hero photo replaces gradient.
- 18:42 CET — Task 14: Sprint 1 complete. All 40 tests green. Opening PR to master.
- 06:45 CET — Task 13: Playwright + axe-core test suites — smoke (routes + redirects), functional (inquiry flow + locale + mailto), a11y (4 pages). Smoke (22 tests) and functional (4 tests) all green. A11y (4 tests) BLOCKED on real source bug: `text-bb-secondary` (#c5a059) fails WCAG 2 AA color-contrast (2.33:1 vs 4.5:1 required) on the `#fcf9f3` background across Eyebrow components site-wide. Test fixes applied: (1) `.first()` on Next button to avoid Next.js dev toolbar conflict, (2) exact labels for Quantity/Event date/Occasion to avoid sidebar aria-label matches, (3) selectOption uses exact string "Year-end".
- 05:30 CET — Task 12: 301-redirected /cosmetics, /food, /textile, /order, /about → new IA; deleted retired routes, components, legacy exports; renamed contact_b2b → contact namespace.
- 04:15 CET — Task 11: Contact 2-step concierge form + sticky inquiry sidebar + mailto submit.
- 00:30 CET — Task 10: editorial pages — Story (3 chapters) + Ateliers (6 cooperatives) + Journal (1 feature + 5 standards).
- 23:55 CET — Task 9: PDP at /product/[id] — sticky image stack, spec column, proof strip, ritual steps, cooperative band, related row.
- 23:15 CET — Task 8: Category page /rituals/[world] with sub-chips + filter rail + sortable grid.
- 22:45 CET — Task 7: Home — Hero + CredentialStrip + EditorialBlock + BentoRituals + Heritage3Up. ShellChrome main top-padding removed; hero pages render edge-to-edge.
- 22:10 CET — Task 6: data layer — 17 products, 3 rituals, full i18n catalogue.
- 21:30 CET — Task 5: Footer + MenuDrawer + InquiryDrawer wired into ShellChrome.
- 21:00 CET — Task 4: Header (sticky transparent→sand) + ShellChrome wrapper.
- 20:30 CET — Task 3: rename cart-context → inquiry-context, storage key migrated to bb.inquiry.
- 20:05 CET — Task 2: primitives — Reveal, Eyebrow, DisplayHeading, Photo, Icon.
- 19:30 CET — Task 1: design tokens + next/font wiring landed.
- 18:15 CET — Sprint 1 implementation plan written: `.project/plans/2026-05-11-stitch-redesign-port.md`. 14 tasks, bite-sized TDD-flavored steps, locked file structure, risk register. Ready for subagent dispatch.
- 17:30 CET — Scope expanded mid-session: user added admin dashboard + SQL DB + analytics ask. Refused to fold into Sprint 1; decomposed into 3 sequential sprints (redesign port → DB+admin → inquiry backend+analytics). Backlog created at `.project/specs/_backlog.md`.
- 17:00 CET — Spec written for Sprint 1 (Stitch redesign port) at `.project/specs/2026-05-11-stitch-redesign-port-design.md`. Locks taxonomy mapping: 3 rituals (Hammam/Botanical/Heritage), `/food` retires (was "Coming soon" placeholder, never shipped), oils + serums migrate into Botanical, existing textile into Heritage.
- 16:45 CET — `.project/` governance scaffolded: `CHANGELOG.md` (this file), `DECISIONS.md`, `specs/_backlog.md`.
- 16:30 CET — Build verified green on cloned `master`: Next 16.2.1 + Turbopack, 5.8s compile, all routes type-clean. One warning: lockfile root inferred wrong (fixed in same commit by setting `turbopack.root` in `next.config.ts`).
- 16:15 CET — Branch `feat/stitch-redesign` cut off `master`.
- 16:00 CET — Repo cloned from `github.com/Taha-ElBouzidi/barbaria-morocco-website` to `D:\dev\Havok\BARBARIA\LATEST\`. `npm install` clean (651 packages, 11 vulns — deferred to follow-up audit).
